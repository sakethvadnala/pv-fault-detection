# Web Framework
fastapi==0.129.0
uvicorn==0.41.0
python-multipart==0.0.22

# ML / Data
tensorflow==2.20.0
keras==3.13.2
scikit-learn==1.8.0
numpy==2.4.2
pandas==3.0.1
joblib==1.5.3

# Supabase (add this — you'll need it!)
supabase==2.15.0

# CORS support (needed for React frontend to talk to backend)
# Already included in fastapi, but confirm starlette is present:
starlette>=0.40.0